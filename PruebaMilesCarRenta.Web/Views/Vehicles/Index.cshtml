@model IEnumerable<PruebaMilesCarRenta.Web.DTO.VehicleDTO>

<div class="row">
	<div class="col-10 ">
		<a asp-controller="Home" asp-action="Index" class="btn btn-secondary btn-flex h-40px border-0 fw-bolder px-4 px-lg-6">Regresar</a>

	</div>
	<div class="col-2">

		<a asp-action="Create" class="btn btn-primary btn-flex h-40px border-0 fw-bolder px-4 px-lg-6">Crear</a>

	</div>

</div>
<div class="row mt-4">
	@foreach (var vehicle in Model)
	{
		@* <div class="col-md-4 mb-4">
			<div class="card">
				<img src="@Url.Content("~/lib/bootstrap/dist/images/" + vehicle.NameImage)" style="width:300px; height:300px; border-radius:50px"   />
				<div class="card-body">
					<h5 class="card-title">@vehicle.Brand - @vehicle.Model</h5>
					<p class="card-text">Año: @vehicle.Year</p>
					<p class="card-text">Cilindraje: @vehicle.CylinderCapacity CC</p>
					<p class="card-text">Precio: $@vehicle.Price</p>
					<a class="btn btn-info btn-sm" asp-action="Details" asp-route-id="@vehicle.Id">Ver Detalles</a>
				</div>
			</div>
		</div> *@
	}
</div>


@* @model IEnumerable<PruebaMilesCarRenta.Web.DTO.VehicleDTO>

@{
	ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div class="row">
	<div class="col-11 ">
		<a asp-controller="Home" asp-action="Index" class="btn btn-secondary btn-flex h-40px border-0 fw-bolder px-4 px-lg-6">Regresar</a>

	</div>
	<div class="col-1">

		<a asp-action="Create" class="btn btn-primary btn-flex h-40px border-0 fw-bolder px-4 px-lg-6">Crear</a>

	</div>

</div>
<div class="row">
	<div class="mt-2" style="padding-left:1%; padding-right:1%;">
		<table id="MyTable" style="width: 100%; overflow-x:auto;" class="table table-striped table-row-bordered table-responsive-xl">

			<thead>
				<tr>
					<th>
						@Html.DisplayNameFor(model => model.NumberPlate)
					</th>
					<th>
						@Html.DisplayNameFor(model => model.Brand)
					</th>
					<th>
						@Html.DisplayNameFor(model => model.Model)
					</th>
					<th>
						@Html.DisplayNameFor(model => model.Year)
					</th>
					<th>
						@Html.DisplayNameFor(model => model.CylinderCapacity)
					</th>
					<th>
						@Html.DisplayNameFor(model => model.Price)
					</th>
					<th>
						@Html.DisplayNameFor(model => model.ImageFullPath)
					</th>
					<th>
						@Html.DisplayNameFor(model => model.Available)
					</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				@foreach (var item in Model)
				{
					<tr>
						<td>
							@Html.DisplayFor(modelItem => item.NumberPlate)
						</td>
						<td>
							@Html.DisplayFor(modelItem => item.Brand)
						</td>
						<td>
							@Html.DisplayFor(modelItem => item.Model)
						</td>
						<td>
							@Html.DisplayFor(modelItem => item.Year)
						</td>
						<td>
							@Html.DisplayFor(modelItem => item.CylinderCapacity)
						</td>
						<td>
							@Html.DisplayFor(modelItem => item.Price)
						</td>
						<td>
							<img src="@Url.Content("~/lib/bootstrap/dist/images/" + item.NameImage)" style="width:100px; height:100px; border-radius:50px"   />

						</td>
						<td>
							@Html.DisplayFor(modelItem => item.Available)
						</td>
						<td>
							<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
							<a asp-action="Details" asp-route-id="@item.Id">Details</a> |
							<a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
						</td>
					</tr>
				}
			</tbody>
		</table>
	</div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");

    }

    <script>
        $(document).ready(function () {
            var buttonCommon = {
                exportOptions: {
                    format: {
                        body: function (data, row, column, node) {
                            // Strip $ from salary column to make it numeric
                            return data;
                        }
                    }
                }
            };
            $('#MyTable').DataTable({

                dom: 'Bfrtip',
                buttons: [
                    $.extend(true, {}, buttonCommon, {
                        extend: 'copyHtml5'
                    }),
                    $.extend(true, {}, buttonCommon, {
                        extend: 'excelHtml5'
                    }),
                    $.extend(true, {}, buttonCommon, {
                        extend: 'pdfHtml5'
                    })
                ],
                aLengthMenu: [
                    [10, 50, 100, 200, -1],
                    [10, 50, 100, 200, "Todos"]
                ]
            });
        });
    </script>


} *@